apiVersion: v1
kind: Service
metadata:
  name: phoenix-sim
  namespace: phoenix-system
  labels:
    app: phoenix-sim
spec:
  selector:
    app: phoenix-sim
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
