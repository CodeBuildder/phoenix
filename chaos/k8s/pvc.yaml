apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phoenix-chaos-history
  namespace: phoenix-system
  labels:
    app: phoenix-chaos
    app.kubernetes.io/managed-by: phoenix
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
