apiVersion: v1
kind: Service
metadata:
  name: phoenix-chaos
  namespace: phoenix-system
  labels:
    app: phoenix-chaos
spec:
  selector:
    app: phoenix-chaos
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
