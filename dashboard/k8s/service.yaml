apiVersion: v1
kind: Service
metadata:
  name: phoenix-dashboard
  namespace: phoenix-system
  labels:
    app: phoenix-dashboard
spec:
  selector:
    app: phoenix-dashboard
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
