apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phoenix-agent-history
  namespace: phoenix-system
  labels:
    app: phoenix-agent
    app.kubernetes.io/managed-by: phoenix
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
